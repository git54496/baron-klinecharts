# Baron KLineCharts 工程协作约束

## 历史 K、实时 K 与产品能力对齐

### 背景

- Baron KLineCharts 当前以宿主投送的历史 K 和版本化 Scene 为主要输入，已经支持首次安装历史 Scene、向前补入更早历史 K、跨周期替换 Scene 和 Drawing 独立持久化。
- `new_cage` 已经具备历史 K 薄 Datafeed、周期与复权参数、历史补页以及跨周期 Scene 装配；实时 K 目前仍处于独立演进阶段。
- 当前产品能力对齐目标是参考 KLineChart Pro，补齐顶部行情栏、左侧绘图栏、主图指标、时区、设置和全屏等能力。该工作不应被完整实时 K 链路阻塞。

### 强制设计约束

1. 产品能力对齐优先在历史 K 基线上完成。除非需求明确依赖实时状态，否则不得以“实时 K 尚未完成”为由推迟工具栏、主图指标、时区、设置或全屏能力。
2. 当前阶段只约束职责边界和演进方向，不提前锁死实时 K 的具体接口、事件名、数据结构或最终化协议。相关具体契约必须在实时 K 专项设计中结合真实调用链收敛。
3. 新增能力必须保持实时兼容：不得把指标、绘图、视口、时区或展示设置与“只存在历史 K”这一假设强绑定，也不得要求未来每次实时更新都重建完整 Runtime。
4. 已确认历史 K 与形成中实时 K 必须被视为不同生命周期的数据。未来接入实时 K 时，形成中 K 应作为可替换的临时投影处理，不得在未明确 Canonical 和导出语义前直接混入权威历史事实。
5. 不得把高频实时 K 更新实现为每次都整体替换完整 Scene。未来实时链路应采用与更新频率匹配的增量或投影机制，并保持 Drawing、指标配置和视口状态稳定。
6. Baron KLineCharts 只负责规范数据接收、图表状态、指标计算、Drawing、渲染和可复用图表 UI；不得直接请求 `fxxking-data`、交易所或其他上游行情源，也不得持有宿主鉴权、订阅、重连和缓存策略。
7. `new_cage` 等宿主负责标的身份、周期、复权、时间范围、历史请求、未来实时订阅、响应身份校验和业务错误处理。所有金融市场数据请求必须通过 `fxxking-data` 发起。
8. 周期切换与复权切换不得被机械合并为同一种状态迁移。周期切换可以在同一价格口径内替换 Scene 并保留 DrawingDocument；复权切换必须尊重不同价格坐标和 DrawingDocument 作用域，是否重建 Runtime 由宿主业务语义决定。
9. 主图指标应由浏览器 Runtime 基于当前规范 OHLCV 序列计算。宿主只提供对应周期和复权口径的数据，不得为了产品 UI 对齐而新增服务端内置指标计算依赖。
10. 指标配置和图表展示状态必须与行情运输方式解耦。历史快照替换、历史补页以及未来实时 K 更新后，指标应基于当前有效数据重新计算，不得静默丢失用户选择。
11. Baron 中的可复用工具栏可以发出周期、复权等宿主意图，但不得自行执行网络副作用。宿主必须能够注入可用项、禁用态、加载态、失败态和实际处理逻辑。
12. 公开能力变更应优先保持向后兼容。若必须调整宿主接入方式，需要同步核对 `new_cage`，但不得把 `new_cage` 的业务类型、接口路径或页面状态下沉到 Baron 公共契约。

### 设计评审检查项

- 本次设计是否无意中假设数据永远只有历史 K。
- 实时 K 接入后是否会迫使工具栏、指标或 Drawing 重新设计。
- 是否清楚区分了已确认历史事实、临时展示投影和用户交互状态。
- 是否把行情请求、鉴权、订阅或重连错误地放进 Baron。
- 周期与复权切换是否分别遵守当前 Scene 和 DrawingDocument 语义。
- 是否通过增量或投影思路为未来实时更新留出空间，而未过早固化具体协议。
- 是否能在完全没有实时数据源时，仅依靠历史 K 完成开发、测试和验收。

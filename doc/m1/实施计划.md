# baron-klinecharts M1 真实标的看图与参考线实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` task-by-task after Task 0 calibration. Steps use checkbox (`- [ ]`) tracking. Repository policy forbids subagents, worktrees, and Git/tag/release mutations without explicit user approval.

**Goal:** 以公共正式版本交付日线蜡烛图、水平参考线和确定 Scene 恢复能力，使 `my-cage` 能从 registry 安装后完成 M1 图表旅程。

**Architecture:** 保留现有 KLineCharts 10 单引擎、Scene Schema → Adapter → Web Runtime 分层。M1 优先验证和发布已有最小能力，不提前实现对数拖动、量度、全屏或完整样式矩阵。

**Tech Stack:** TypeScript、JSON Schema、KLineCharts 10.0.0、Vitest、Playwright、npm workspaces、Python、GitHub Actions、npm/PyPI。

**关联文档：**

- 本仓目标说明：`目标说明.md`
- 本仓执行前校准：`执行前校准报告.md`
- 总体章程：`../../../doc/overall/三仓联合总目标章程.md`
- 纵向协作设计：`../../../doc/overall/纵向里程碑协作设计.md`
- 总校准结论：`../../../doc/m1/总校准结论.md`
- 跨仓验收：`../../../doc/m1/跨仓验收卡.md`

---

## Task 0：执行前校准

**Files:**

- Read: `package.json`
- Read: package manifests
- Read: `doc/m1/目标说明.md`
- Read: `doc/m1/执行前校准报告.md`
- Read: `docs/superpowers/specs/2026-07-24-klinecharts-scene-platform-design.md`
- Read: `docs/superpowers/specs/2026-07-27-public-release-design.md`
- Inspect: Scene/Adapter/Runtime implementation and tests

- [x] **Step 1: 记录工作区**

```bash
git status --short
git branch --show-current
git log -1 --oneline
git tag --list
```

Expected:

- 记录 HEAD 与所有未提交改动；
- 不覆盖并行 coding agent 的文件；
- 不清理已有 `dist/`、test-results 或本地环境，除非计划明确要求且用户授权。

- [x] **Step 2: 运行质量基线**

```bash
fnm use
npm ci
python3 -m venv <temporary-venv>
source <temporary-venv>/bin/activate
python -m pip install -e 'python/baron-klinecharts[dev]'
npm run verify
npm run test:browser --workspace @baron1996/klinecharts-adapter
npm run test:browser --workspace @baron1996/klinecharts-runtime
npm run test:browser --workspace @baron1996/klinecharts-render-runtime
```

Expected:

- 使用 `.nvmrc` 指定的 Node `22.12.0`，不降低 `engine-strict`；
- 本地基线与 CI 一样，必须先安装当前 Python 项目再执行 `npm run verify`；
- 记录每个子命令结果；
- 根 `npm run verify` 当前不包含三个 package 自己的 Playwright suite，因此 Task 0 和发布候选门必须显式运行；
- 原有失败不通过更新 baseline、跳过测试或降低审计级别规避。

- [x] **Step 3: 盘点 M1 已有能力**

逐项检查：

- `packages/scene-schema/schema/chart-config.schema.json`
- `packages/scene-schema/schema/overlay.schema.json`
- `packages/scene-schema/src/semantic-validator.ts`
- `packages/scene-schema/src/canonicalize.ts`
- `packages/klinecharts-adapter/src/conversion/overlays.ts`
- `packages/klinecharts-adapter/src/registry/overlays.ts`
- `packages/klinecharts-adapter/src/adapter.ts`
- `packages/web-runtime/src/runtime.ts`
- `packages/web-runtime/src/types.ts`
- `packages/web-runtime/src/toolbar/toolbar-tools.ts`
- `packages/web-runtime/src/toolbar/standard-toolbar.ts`
- 相关 tests。

输出差距表：

| 能力 | 当前实现 | 当前测试 | M1 缺口 | 计划文件 |
|---|---|---|---|---|
| 蜡烛图 | Scene 强制唯一 candle pane、递增日线和合法 OHLC；Adapter 映射完整 candle styles，并只加载内嵌静态数据 | Schema、DataLoader、Adapter 初始化、minimal/dark PNG baseline 通过 | 只需增加 M1 专项 fixture/契约证据，不修改蜡烛图实现 | Task 1、Task 3 |
| 水平参考线 | `horizontalStraightLine` 已注册；正常输入和 Adapter 导出都使用 `anchor: { value }` | 21 类型 round-trip、真实浏览器创建/恢复诊断通过 | **真实源码缺口：TypeScript/Python 语义校验仍接受 `{ timestamp, value }`**；需双语言负例和最小修复 | Task 1 |
| Stable ID | Runtime 确定生成或接受调用方 ID；Adapter 把 Scene ID 直接传给引擎并校验返回 ID | deterministic drawing ID、21 类型 round-trip、M1 浏览器诊断通过 | 增加正式的 create/export/destroy/recreate 稳定 ID 回归，不重复实现 ID 系统 | Task 2、Task 3 |
| Scene round-trip | 完整 Scene export、RFC 8785 canonical bytes、SHA-256、双语言实现已存在；styles/metadata/paneId 由源快照恢复 | canonical、cross-language、offline HTML、M1 浏览器诊断通过 | 增加 M1 水平线跨生命周期 hash 与字段等值的常驻测试 | Task 1、Task 2、Task 3 |
| 工具栏 | 已有水平直线入口、当前选择删除、canonical Scene 导出、键盘焦点和触摸路径 | Runtime toolbar 7/7、根浏览器 6/6、M1 创建/选择/删除诊断通过 | 只需把水平线 M1 旅程固化为正式浏览器测试 | Task 3 |
| 正式发布 | 五个 workspace、Python、Runtime assets 均为 `0.1.0`，Scene version 为 `1`；四个公共 npm 包和一个 Python 包的构建/安装流水线已存在 | `npm run verify`、安装 24/24、package browser 17/17 通过 | registry 未发布；外部配置/授权待确认；根 verify 必须纳入 package browser suites | Task 4、Task 5、Task 6、Task 7 |

- [x] **Step 4: 重新核验 registry**

```bash
npm view @baron1996/kline-scene-schema versions --json
npm view @baron1996/klinecharts-adapter versions --json
npm view @baron1996/klinecharts-runtime versions --json
npm view @baron1996/klinecharts-cli versions --json
python -m pip index versions baron-klinecharts
gh release list
```

依据实际状态确定首个未占用统一版本。不得覆盖已发布版本。

- [x] **Step 5: 记录 M1 决策状态**

确认：

- Scene version；
- 正式水平线类型，原则上 `horizontalStraightLine`；
- M1 默认 line style；
- M1 标准工具栏入口；
- npm/PyPI/GitHub 外部配置；
- 首发 token/OIDC 路径；
- 发布授权时间点。

- [x] **Step 6: 校准计划**

已有能力若完整，则保留测试/发布步骤并删除重复编码步骤。只有测试证明缺口时才修改实现。

Expected: `BK-M1-0` 可复核。

### Task 0 校准结论

- 当前校准基线：`main@82564491566afb213c27faed57ae4aef144f1991`，本地与远端均无 tag，GitHub 无 Release。
- npm/Python 首发候选版本确认为 `0.1.0`；Scene version 确认为 `1`；Runtime version 确认为 `0.1`；KLineCharts 保持 `10.0.0`。正式发布前仍须重新查询 registry，避免并发占用。
- 正式水平参考线类型确认为 `horizontalStraightLine`，唯一有意义的几何字段是有限数值 `anchor.value`；必须拒绝无意义 `timestamp`，不得保存像素或数组索引。
- 默认样式已经由用户确认为现有蓝色 `rgba(41, 98, 255, 1)`、`1px`、`solid`，M1 不新增样式系统工作。
- npm `release` Environment、组织权限、2FA、首发 Granular Access Token、PyPI Pending Trusted Publisher、后续 npm Trusted Publisher/OIDC 和实际发布授权均无可核验证据，状态保持 `DECISION_REQUIRED`。
- Task 0 的产品与契约选择已经关闭；Task 1 仍须用户单独明确授权。外部配置、registry 再检查及 tag、Release、npm/PyPI 发布继续作为发布门禁，不得写成已完成。

---

## Task 1：冻结 M1 Scene fixture

**范围约束：** Task 1 只修复已经证实的 TypeScript/Python `horizontalStraightLine.anchor.timestamp` 语义校验缺口，并补齐对应的永久回归证据。不得重写蜡烛图、Overlay ID、Adapter、Runtime、canonicalization 或工具栏框架。

**Files:**

- Create: `tests/fixtures/scenes/m1-candle-horizontal-line.json`
- Modify: `packages/scene-schema/test/overlay-schema.spec.ts`
- Modify: `packages/scene-schema/test/semantic-validator.spec.ts`
- Modify: `packages/scene-schema/test/canonicalize.spec.ts`
- Modify: `packages/scene-schema/test/canonical-json.spec.ts`
- Modify: `tests/cross-language/chart-scene-roundtrip.mjs`
- Modify only because Task 0 proved the contract gap:
  - `packages/scene-schema/src/semantic-validator.ts`
  - `python/baron-klinecharts/src/baron_kline/validation.py`
  - `python/baron-klinecharts/tests/test_validation.py`
- Generate: TypeScript/Python/runtime derived assets only if source Schema changes

- [x] **Step 1: 创建有效 M1 fixture**

Fixture 包含：

- 一只中性示例 symbol；
- `period=day`；
- 至少一段合法 OHLCV；
- candle pane；
- 一个 `horizontalStraightLine`；
- 固定 Overlay ID；
- 固定 price `value`；
- 固定 styles；
- 可选不透明 metadata。

Fixture 不包含 `my-cage` 的 `confirmed`、`levelType`、结构或规则字段。

- [x] **Step 2: 写 Schema 测试**

断言：

- fixture 有效；
- 水平线缺少 ID、paneId、value 或 styles 时无效；
- 水平线 `anchor` 含 timestamp 时无效，必须严格为 `{ value }`；
- NaN/Infinity 无效；
- 非法 pane 引用无效；
- metadata 原样往返。

- [x] **Step 3: 写 canonical 测试**

重复 parse/canonicalize：

- canonical JSON 相同；
- SHA-256 相同；
- Overlay ID、value、styles、metadata 相同。

- [x] **Step 4: 运行确认现状**

```bash
npm run test --workspace @baron1996/kline-scene-schema
npm run test:cross-language
```

Expected:

- 如果现有 Schema 已满足，测试直接 PASS；
- 如果失败，只补 M1 契约缺口。

- [x] **Step 5: 生成**

仅当 Schema 源文件改变时运行：

```bash
npm run generate
```

不得直接编辑 generated 或 `dist/`。

Task 1 执行时修改了 Scene Schema 包的语义校验源码，因此完整 `npm run verify` 的内置 generate 阶段同步更新了 Runtime 派生资产；没有直接编辑 generated、`dist/` 或发布制品。

---

## Task 2：证明 Adapter 水平参考线 round-trip

**Files:**

- Modify: `packages/klinecharts-adapter/test/overlays.browser.spec.ts`
- Modify: `packages/klinecharts-adapter/test/roundtrip.browser.spec.ts`
- Modify only if tests prove gap:
  - `packages/klinecharts-adapter/src/conversion/overlays.ts`
  - `packages/klinecharts-adapter/src/conversion/id-map.ts`
  - `packages/klinecharts-adapter/src/adapter.ts`

- [x] **Step 1: 写创建测试**

浏览器创建 M1 Scene，调用 Adapter 添加水平线，断言：

- engine ID 等于 Scene stable ID；
- export 后 type/id/pane/value/styles/metadata 一致；
- 没有像素坐标或数组 index 写入 Scene。

- [x] **Step 2: 写销毁重建测试**

```text
create adapter
→ export scene
→ dispose
→ create new adapter with export
→ export again
```

断言：

- Overlay 数量为 1；
- ID、value、style 和 metadata 相同；
- container 生命周期干净；
- 原容器样式按现有契约恢复。

- [x] **Step 3: 运行确认 RED/GREEN**

```bash
npm run test:browser --workspace @baron1996/klinecharts-adapter
```

如果现有实现通过，记录 PASS，不修改 Adapter。

- [x] **Step 4: 最小补齐**

只有测试失败时修改转换或 ID map。不得：

- 引入 `my-cage` 类型；
- 保存 screen coordinate；
- 暴露原始 Chart；
- 改用第二引擎。

- [x] **Step 5: 回归**

```bash
npm run test --workspace @baron1996/klinecharts-adapter
npm run test:browser --workspace @baron1996/klinecharts-adapter
npm run typecheck --workspace @baron1996/klinecharts-adapter
```

Expected: PASS。

---

## Task 3：证明 Runtime 与标准工具栏 M1 旅程

**Files:**

- Modify: `packages/web-runtime/test/runtime.browser.spec.ts`
- Modify: `packages/web-runtime/test/toolbar.browser.spec.ts`
- Modify: `tests/browser/edit-and-export.spec.ts`
- Modify only if tests prove gap:
  - `packages/web-runtime/src/runtime.ts`
  - `packages/web-runtime/src/types.ts`
  - `packages/web-runtime/src/toolbar/toolbar-tools.ts`
  - `packages/web-runtime/src/toolbar/standard-toolbar.ts`
  - `packages/web-runtime/src/lifecycle.ts`

- [x] **Step 1: 写 Runtime 旅程测试**

```text
create runtime
→ start horizontalStraightLine drawing
→ complete drawing
→ get/list overlay
→ export scene
→ destroy
→ recreate runtime
→ select and remove overlay
```

断言：

- 创建事件只产生一条线；
- stable ID 可由调用方指定或确定生成；
- export/recreate 保持 value/style/metadata；
- 删除只删除目标线；
- destroy 后 API 明确失败且不再发事件。

- [x] **Step 2: 写工具栏测试**

通过真实 DOM 操作：

- 打开水平线工具；
- 在图表点击形成线；
- 选择该线；
- 删除当前选择；
- 导出 Scene；
- 键盘焦点可达；
- 触摸基础路径可用。

- [x] **Step 3: 写页面刷新等价测试**

本仓不实现浏览器数据库持久化，使用：

```text
export Scene
→ serialize
→ page/runtime teardown
→ deserialize
→ recreate
```

证明刷新恢复所需图表契约成立。

- [x] **Step 4: 运行**

```bash
npm run test --workspace @baron1996/klinecharts-runtime
npm run test:browser --workspace @baron1996/klinecharts-runtime
npm run test:browser
```

- [x] **Step 5: 最小补齐**

仅补测试证明的 M1 缺口。不实现确认按钮、对数轴、量度、全屏或完整样式。

Expected: `BK-M1-1`、`BK-M1-2` PASS。

---

## Task 4：建立 M1 干净消费者契约

**Files:**

- Create: `tests/installation/m1-runtime-consumer.test.mjs`
- Modify: `tests/installation/fresh-install.spec.mjs`
- Modify: `tests/installation/npm-pack.spec.mjs`
- Modify: package READMEs

- [x] **Step 1: 写 tarball 消费测试**

在临时 Node 22 工程中安装四个公共 tarball，断言：

- 无 workspace link；
- 无源码路径；
- 可以导入 Schema 和 Runtime；
- M1 fixture 校验；
- 浏览器 smoke 能创建并恢复水平线。

- [x] **Step 2: 写 public API 边界测试**

消费者只使用 package exports，不访问：

- `packages/*/src`；
- Adapter 内部 Chart；
- render-runtime private workspace；
- 测试 fixture 之外的本地路径。

- [x] **Step 3: 运行**

```bash
npm run test:installation
```

Expected: PASS。

- [x] **Step 4: README**

增加 M1 最小示例：

- 构建 ChartScene；
- 创建 Runtime；
- 画水平线；
- export；
- recreate。

示例不得请求行情或包含业务确认。

---

## Task 5：构建发布候选

**Files:**

- Modify version manifests only if Task 0 selects a new version
- Modify: `package.json`，把 Adapter、Runtime、Render Runtime 的 package browser suites 纳入根 `verify`
- Modify: lockfile
- Modify: release notes/README
- Use: existing release scripts and workflows

- [x] **Step 1: 同步版本**

根 workspace、五个 workspace、Python、Runtime assets 和 tag 预期版本一致。render-runtime 保持 private。

- [x] **Step 2: 生成**

```bash
npm run generate
```

- [x] **Step 3: 全量验证**

```bash
npm run verify
npm run test:browser --workspace @baron1996/klinecharts-adapter
npm run test:browser --workspace @baron1996/klinecharts-runtime
npm run test:browser --workspace @baron1996/klinecharts-render-runtime
```

Expected: 所有 typecheck、unit、根浏览器、package browser、rendering、Python、cross-language、installation 和 audit 通过；发布工作流调用的根 `verify` 不得遗漏 package browser suites。

- [x] **Step 4: 版本门**

```bash
npm run release:check-version -- --tag v<confirmed-version>
```

- [x] **Step 5: 构建 npm artifact**

```bash
npm run release:build-npm
```

同时按现有 Python 构建流程生成 wheel/sdist。

- [x] **Step 6: 检查内容**

确认：

- 正好四个公共 npm tarball；
- render-runtime 不发布；
- artifact version 一致；
- SHA-256 清单；
- 无 workspace link、本地绝对路径、测试结果、录屏、`.env` 或秘密；
- 许可证和 NOTICE 完整。

- [x] **Step 7: 再次干净安装**

使用最终 artifact 运行 M1 consumer test。

Expected: `BK-M1-3` PASS，状态为“发布候选”，不是正式发布。

---

## Task 6：正式发布

**External mutation gate:** 本任务包含 Git tag、GitHub Release、npm 和 PyPI 发布，必须先获得用户明确授权。

- [x] **Step 1: 复核外部配置**

确认：

- GitHub `release` Environment；
- 审批和 tag 限制；
- npm 组织权限与 2FA；
- 首发 token 或已存在包的 Trusted Publisher；
- PyPI Pending Trusted Publisher/OIDC；
- 所有 registry 目标版本尚不存在。

- [x] **Step 2: 使用已验证 artifact**

发布工作流必须从用户批准的 tag 对应提交构建一次或复用其不可变 artifact，不能为失败重试构建不同内容。

- [x] **Step 3: 按依赖顺序发布 npm**

顺序：

1. scene-schema；
2. adapter；
3. runtime；
4. CLI。

- [x] **Step 4: 发布 Python 与 Release**

保持版本一致，上传 artifact 和 SHA-256。

- [x] **Step 5: 完成 Trusted Publisher 切换**

若这是首个 npm bootstrap 版本，按既有发布设计验证 OIDC 后删除一次性 token。不保留 token fallback。

- [x] **Step 6: 处理部分发布**

部分包成功时：

- 不覆盖已发布包；
- 不改变版本内容；
- 使用同一 artifact 重试未完成 job；
- 记录真实 registry 状态。

Expected: npm/PyPI/tag/Release 一致。

---

## Task 7：registry 黑盒与总 session 交付

- [ ] **Step 1: 从 registry 安装**

```bash
npm install \
  @baron1996/kline-scene-schema@<version> \
  @baron1996/klinecharts-adapter@<version> \
  @baron1996/klinecharts-runtime@<version>
```

不得使用 npm cache 中的本地 tarball冒充网络安装；记录 registry resolved URL 和 integrity。

- [ ] **Step 2: 运行 M1 黑盒**

只通过公开 API：

- 校验 M1 fixture；
- 创建 Runtime；
- 画水平线；
- export；
- destroy/recreate；
- 比较 ID/value/style/metadata/hash；
- 删除。

- [ ] **Step 3: `my-cage` 边界检查**

确认 `my-cage`：

- lockfile 是精确 registry 版本；
- 无 `file:`、workspace、Git 或源码路径；
- 无其他图表引擎；
- 无复制本仓源码；
- 只使用 Runtime 公共 API。

发现问题只报告给总 session，由消费者 session 修改。

- [ ] **Step 4: 回传**

返回：

1. Task 0 差距表；
2. 统一版本；
3. Scene version；
4. npm/PyPI/tag/Release 链接；
5. artifact hash/integrity；
6. `npm run verify` 结果；
7. M1 浏览器旅程；
8. registry 黑盒；
9. 公开 API 与非目标；
10. 未获授权而未执行的动作。

Expected: `BK-M1-4` 可由总 session 判定。

---

## 最终清单

- [ ] Task 0 校准完成。
- [ ] M1 Scene fixture 通过。
- [ ] 蜡烛图通过。
- [ ] 水平参考线 stable ID/value/style/metadata 通过。
- [ ] Adapter round-trip 通过。
- [ ] Runtime export/destroy/recreate 通过。
- [ ] 工具栏 M1 旅程通过。
- [ ] 生命周期无泄漏。
- [ ] 干净 tarball 消费通过。
- [ ] `npm run verify` 通过。
- [ ] 正式 npm/PyPI/tag/Release 已发布。
- [ ] registry 黑盒通过。
- [ ] `my-cage` 依赖边界通过。
- [ ] 未提前实现 M2/M3 能力。
- [ ] 未执行未经授权的 Git、发布或外部状态变更。

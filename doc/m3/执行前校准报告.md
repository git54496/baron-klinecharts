# baron-klinecharts M3 Task 0 执行前校准报告

> 结论：`READY_WITH_CORRECTIONS`  
> 校准日期：2026-08-06（Asia/Shanghai）  
> 基线：`main@27a7f910…`（v0.4.3）

## 1. 工作区

- 分支/HEAD：`main` 与 `origin/main` 同步，HEAD `27a7f91`（tag `v0.4.3`）；
- 脏工作区：仅既有未跟踪制品目录（`.release-candidate-*`、`release-candidate-final-artifacts/`、`.DS_Store`、`doc/` 杂项）与新增 `doc/m3/`；无未提交源码改动；
- 发布状态：npm 三包与 PyPI 均 `0.4.3`，GitHub Release `v0.4.3`。

## 2. 已具备能力（0.3.0～0.4.3）

- 时间序列协议与跨语言运行时（`time-series` 包、`time-series-types`）；
- 指标注册：`SUPPORTED_INDICATORS` 已含 `MA`、`VOL`、`EMA`、`SMA`、`BBI`、`MACD` 等，adapter 已有 indicator snapshot/映射与指标 pane 边界；
- 主系列呈现：蜡烛/收盘折线切换（`MainSeriesPresentation`、`STANDARD_CLOSE_LINE_PRESENTATION`）已有实现与浏览器测试；
- 十字线：时间序列事件已含 `crosshair-changed`；
- 绘图矩阵：水平线、量度、cross-line、rectangle、arrow、text 等交互与命中/拖动/仲裁修复（0.4.1～0.4.3）已完成；
- Scene 恢复、canonical、跨语言产物与发布链已闭环。

## 3. M3 缺口与建议

| 缺口 | 现状 | Task 1 建议契约 |
|---|---|---|
| MA18/45/60/200/250 独立开关 | 引擎/注册层支持 MA，未见 my-cage 五条独立开关的消费方契约 | 冻结指标 DTO：五条 MA 独立 id/开关/样式，Scene 持久化与恢复 |
| VOL 与 MAVOL5/10/20 | 注册层支持 VOL；未冻结 MAVOL 参数映射与副图布局 | 冻结 VOL 副图 + MAVOL5/10/20 参数、样式与恢复 |
| 全屏 | 未发现 fullscreen 能力引用 | 冻结全屏 API/事件与恢复语义 |
| 十字线 OHLCV 信息 | `crosshair-changed` 事件已存在 | 冻结事件载荷字段（OHLCV、时间戳、pane）与订阅契约 |
| 指标场景恢复 | 指标 snapshot 已存在，恢复契约未冻结 | 冻结指标加入/移除/参数变更/恢复的 Scene canonical |

## 4. 对外契约建议

- 指标通过 Scene/Indicator DTO 表达，不把 my-cage 业务语义写入图表仓；
- 全屏、十字线以正式 Runtime 事件/API 交付，my-cage 不自行实现；
- 保持 Scene version 与 Runtime protocol 的显式版本化；如协议变化，按既有流程升级版本并冻结兼容边界。

## 5. 风险与待决策

- MAVOL 是否以 VOL 指标参数表达还是独立指标名，需 Task 1 冻结；
- 全屏与现有宿主布局（my-cage 页面）的交互边界需用户确认；
- 发布新版本（预计 0.5.x）需单独授权。

## 6. Task 1 可执行边界

- 冻结指标、全屏、十字线与绘图矩阵的最小公开契约与浏览器验收样本；
- 确认 MAVOL 表达方式与指标版本策略；
- 不开始业务实现、不发布。
